## Implemented

- `NodeIdentity` and `EdgeIdentity` value types in `src/model/mod.rs`.
- Public `stable_node_id` and `stable_edge_id` helpers in `src/graph/mod.rs`.
- Identity-derived SHA-256 fixed-width IDs. Absolute roots do not enter identity input; normalized relative paths do.
- `Graph::resolve_cross_file(&mut self) -> Result<ResolutionReport>` with resolved, unresolved, and ambiguous call counts.
- Conservative ambiguity behavior: ambiguous and unresolved calls produce no fabricated edge.
- `Graph::validate` checks duplicate IDs, duplicate identities, dangling endpoints, identity-derived IDs, and canonical ordering.
- Rust, Python, and TypeScript cross-file fixtures plus `tests/integration_m01.rs`.

## Evidence

`cargo test --test integration_m01 -- --nocapture`: 4 passed, 0 failed.

`cargo test --lib graph -- --nocapture`: 3 passed, 0 failed.

`cargo test --all-targets --all-features`: passed.

`cargo fmt --check`: passed.

`cargo check --all-targets --all-features`: passed.

`cargo clippy --all-targets --all-features -- -D warnings`: passed.

## Remaining Gaps

Resolver uses current parser IR and M0 syntactic call facts. Richer import-backed module-qualified symbol extraction remains outside this task and belongs to later parser/query work.

## Round 1 Fixes

- `Graph::resolve_cross_file` now performs import-backed resolution from retained parser call facts, emits unique imported targets as `Inferred`, and reports ambiguous/unresolved calls without fabricated edges.
- Integration tests cover actual Rust, Python, and TypeScript fixture resolution plus ambiguous imports.
- Node IDs include full source location through end column. Edge IDs distinguish `None` from `Some("")` labels.
- Removed unused node-ID state; duplicate IDs remain validation errors.
- Root-independent identity test uses distinct absolute roots ending at the same repository-relative path.

Round-1 verification: `cargo test --test integration_m01 -- --nocapture` passed 4 tests; `cargo test --all-targets --all-features` passed; `cargo fmt --check` passed; `cargo check --all-targets --all-features` passed; `cargo clippy --all-targets --all-features -- -D warnings` passed.
