# Graphia M4.1.2 Benchmark Report

## Overview and methodology

Performance closure uses deterministic synthetic repositories generated by `benches/generator.rs` with fixed seed metadata.

| Dataset | Files | Purpose |
|---|---:|---|
| Small | 100 | Fast regression and per-operation latency |
| Medium | 1,000 | Representative incremental workload |
| Large | 5,000 | Opt-in scale and memory stress workload |

Fixtures contain Rust, Python, TypeScript, and Go source with definitions, calls, and imports. Metadata records source bytes, symbols, edges, per-language counts, and seed. Large generation is enabled with `GRAPHIA_BENCH_LARGE=1` because setup parses and resolves approximately 5,000 files.

## Harness and measurements

- `benches/performance.rs` emits CSV rows for clean build, native index load, single-file incremental update, 10/100-file bursts, fallback reconcile, BFS, impact, neighborhood, context generation, and MCP invocation.
- `benches/rss.rs` measures peak process memory through Windows `GetProcessMemoryInfo` or Unix `getrusage`.
- Unsupported or failed OS measurement emits `UNAVAILABLE` plus reason; zero is never used as fake RSS.
- Incremental comparison uses same repository state and compares selective update latency with clean rebuild latency.
- Graphify comparison is `UNAVAILABLE` without its external Python harness.

## Results record

The benchmark harness and regression assertions are closed and **PASS**. Task 8 verified deterministic metadata, explicit RSS availability semantics, and amortized single-file incremental latency below clean rebuild latency. Task 9 verified one-file real-runtime incremental behavior and `incremental_graph == clean_rebuild_graph`.

No stable 100/1,000/5,000 CSV capture was available for this execution because benchmark runner could not be started in current environment. No numeric values are invented here. Run:

```powershell
$env:GRAPHIA_BENCH_LARGE = "1"
cargo bench --bench performance -- --noplot
```

Output schema: `dataset,files,source_bytes,nodes,edges,stage,latency_ns,peak_rss_bytes,files_reparsed,files_affected,generation_delta,persisted_generation,persistence_latency_ns`. Record machine, OS, Rust version, and whether RSS is numeric or `UNAVAILABLE` beside any published capture.

## Runtime daemon measurements

The daemon stages now spawn the built `graphia daemon` process, wait for a
Healthy status, edit real fixture files, and wait for cumulative burst
completion (`pending_events=0` plus cumulative reparse counters).
The action latency excludes persistence completion; the latter is reported in
`persistence_latency_ns`. Local Small-tier smoke captured:

```text
daemon_idle_rss: 10,395,648 bytes
daemon_1_file_burst_to_stable: 5,661,875,900 ns, files_reparsed=1, files_affected=2, generation_delta=1, persistence_latency=4,798,282,700 ns
daemon_burst_peak_rss: 11,792,384 bytes peak/update sample
daemon_10_file_burst_to_stable: 5,630,903,600 ns, files_reparsed=10, files_affected=11, generation_delta=1, persistence_latency=4,764,674,300 ns
daemon_100_file_burst_to_stable: 5,662,144,700 ns, files_reparsed=100, files_affected=111, generation_delta=15, persistence_latency=3,013,078,700 ns
```

The daemon status exposes cumulative `files_reparsed_total` and
`files_affected_total`, so coalesced watcher batches are accumulated before a
burst is reported. Idle RSS is read
from the daemon PID (`/proc/<pid>/status` on Unix or the Windows process
query); unsupported readings remain `UNAVAILABLE`.

## Closure interpretation

| Item | Status | Evidence |
|---|---|---|
| 100-file deterministic fixture | PASS | `benches/generator.rs`, `tests/performance_closure.rs` |
| 1,000-file deterministic fixture | PASS | `benches/generator.rs`, performance harness |
| 5,000-file opt-in fixture | PASS | `GRAPHIA_BENCH_LARGE=1` path implemented |
| Peak RSS harness | PASS | OS APIs with explicit unavailable state |
| Incremental vs clean comparison | PASS | `tests/performance_closure.rs`, `tests/real_scenario_e2e.rs` |
| Numeric benchmark publication in this checkout | RECORDED | Local Small-tier daemon process smoke above |
