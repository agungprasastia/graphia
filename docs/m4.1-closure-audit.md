# Graphia M4.1 — Foundation Closure & Integrity Hardening Gap Audit

| Area | Claim | Actual implementation | Gap | Severity | Required action | Status |
|---|---|---|---|---|---|---|
| **Linting & Code Hygiene** | Strict 0 compiler and clippy warnings | `Cargo.toml` has `missing_docs = "allow"`; `target/` not enforced cleanly without warnings | Explicit suppression hides missing docs/warnings | P0 | Remove `missing_docs = "allow"`, ensure 0 warnings under `-D warnings` | IN_PROGRESS |
| **Semantic Identity** | Stable, deterministically hashed symbol identity across line shifts | `NodeIdentity` contains `SourceLocation` (start/end line & col). Editing lines shifts NodeId | Line shifts change NodeId even if signature/name unchanged | P0 | Redesign `SemanticNodeKey` without physical line/col; add signature, container, language, module | PENDING |
| **Semantic Model: NodeKind** | Comprehensive semantic entity kinds for 16 languages | `NodeKind` only has 8 kinds (`File`, `Module`, `Function`, `Method`, `Class`, `Struct`, `Trait`, `Interface`) | Missing `Enum`, `Constructor`, `Namespace`, `Package`, `Variable`, `Constant`, `Field`, `Property`, `TypeAlias` | P0 | Expand `NodeKind` enum, update AST parsers to extract true kinds | PENDING |
| **Semantic Model: EdgeKind** | Graph contains detailed syntax and semantic relations | `EdgeKind` only has 5 kinds (`Contains`, `Imports`, `Calls`, `Inherits`, `Implements`) | Missing `Exports`, `References`, `TypeReferences`, `Instantiates` | P0 | Expand `EdgeKind` enum, update resolver to generate these relations | PENDING |
| **Semantic Model: Confidence** | Accurate relationship certainty semantics | `Confidence` only has `Extracted` and `Inferred` | Missing `Resolved` (deterministic linker) and `Possible` (multi-candidate dynamic dispatch) | P0 | Expand `Confidence` enum to `Extracted`, `Resolved`, `Inferred`, `Possible` | PENDING |
| **Semantic Metadata** | Rich symbol signature, container, visibility | `Node` struct has name, qualified_name, location, language; no visibility or signature | Cannot distinguish overloads or public APIs accurately | P0 | Add `visibility: Visibility`, `signature: Option<String>`, `container: Option<String>` to `Node` | PENDING |
| **Parser IR** | Normalized extraction representation across 16 languages | `ParsedFile` only holds `symbols`, `imports`, `calls` | Lacks definitions/references distinction, exports, type references, signatures | P0 | Refactor parser output to `definitions`, `references`, `imports`, `exports`, `calls`, `type_references` | PENDING |
| **Language Coverage Matrix** | 16 languages fully supported | Parsers extract basic constructs; C headers blindly mapped to C, enums mapped to Structs | Varied depth of extraction across 16 languages | P0 | Audit all 16 parsers, implement missing entities, document in `docs/language-support.md` | PENDING |
| **Resolution Engine** | Resolution beyond call graph | Resolver primarily resolves `Calls` edges; imports and types not resolved to typed edges | Missing type reference, re-export, instantiation, overload resolution | P0 | Extend resolver to resolve imports, type references, exports, instantiations with overloads | PENDING |
| **Incremental Engine** | Truly incremental affected-file updates | `update_repository()` performs full repo scan, full graph build, full index write | Cached reparsing, not affected-file closure graph update | P0 | Implement `IncrementalWorkspace`, `apply_changes(actions)`, affected closure re-resolution | PENDING |
| **Daemon Event Pipeline** | Live debounced incremental synchronization | `reconcile()` called ignoring `SemanticAction`s, errors swallowed with `let _ = ...` | Event drain ignored, errors swallowed, health state simple boolean | P0 | Feed semantic actions to incremental updater, handle health states (`Healthy`, `Dirty`, `Failed`, etc.) | PENDING |
| **Daemon Persistence & OS Signals** | Bounded asynchronous persistence, graceful shutdown | Sync persistence during update, `ShutdownSignal` internal atomic without OS hooks | Blocks update, no real Ctrl+C / SIGINT capture, inert `--foreground` flag | P0 | Implement dedicated persistence worker, wire `ctrlc`/OS signal, clean up `--foreground` | PENDING |
| **MCP Server Reliability** | Safe read-only protocol integration | Loads index without staleness check, internal `expect()` calls, fake cancellation logging | Stale graph served, panics on corrupted state, request cancellation is inert | P0 | Add staleness check, `--auto-index` CLI flag, hard limits with `InvalidParams`, structured errors | PENDING |
| **Advanced Analysis: Graph Diff** | Semantic structural diffing | Edge diff only checks `new_edges.len - old_edges.len`; node diff by `qualified_name` | Miscounts replaced edges, collisions on overloads, location moves treated as replacements | P0 | Implement real edge-set diff by semantic key, detect signature/visibility/location changes | PENDING |
| **Advanced Analysis: API Diff** | Public API signature and breaking change detection | `modified_signatures` always returns `Vec::new()`; public API uses simple underscore heuristics | Missing signature diff, heuristic public visibility | P0 | Implement signature diff and language-aware public API detection | PENDING |
| **Advanced Analysis: Git History** | File churn additions/deletions tracking | `additions` and `deletions` in `FileChurn` always return `0`; empty on git error | Inaccurate churn statistics, silent failure | P0 | Use `git log --numstat`, parse additions/deletions, structured error on failure | PENDING |
| **Advanced Analysis: Typeflow** | AST-aware intra-procedural type flow | Textual regex/line-based heuristics in `typeflow.rs`, parameter flows stubbed as empty | Inaccurate type propagation, empty parameter flow | P0 | Implement AST-aware type flow extraction with `Known`, `Partial`, `Unknown` states | PENDING |
| **Advanced Analysis: Dataflow** | Precise source-to-sink data flow paths | BFS over `Calls`, `Contains`, `Imports` (structural reachability, not data flow) | False claim of data flow for structural path reachability | P0 | Implement intra-procedural & bounded inter-procedural flow graph; keep structural path separate | PENDING |
| **Advanced Analysis: Dynamic Dispatch** | Conservative virtual dispatch candidate resolution | Fallback marks all same-name methods globally as `Possible` | Noisy candidate sets | P1 | Refine candidate ranking using receiver type, arity, imports, traits/interfaces | PENDING |
| **Advanced Analysis: Dead Code** | Accurate unused code candidate detection | Incoming edge counts only, treats public/entrypoints equally | High false positive rate | P1 | Exclude public APIs, entrypoints, trait methods; provide reasons and confidence | PENDING |
| **Advanced Analysis: Boundaries** | Architecture layer boundary checking | Substring path matching prone to false positives | Inaccurate layer assignment | P1 | Implement glob/normalized path segment matching with explicit precedence | PENDING |
| **Benchmark Suite** | Comprehensive performance & memory measurements | `daemon_live_update` only measures snapshot clone; tiny 3/12/48 file fixtures; no RSS | Misleading metric names, unrepresentative datasets, lack of memory profiling | P0 | Rename metrics, add medium/large synthetic datasets, record peak RSS, add Graphify harness | PENDING |
| **Documentation & Reports** | Truthful milestone documentation | Historical reports claim zero-copy serialization, full scopes, atomic bg persistence | Technical claims exceed implementation reality | P1 | Audit and correct all milestone reports (M0.5, M1, M1.1, M3, M4) | PENDING |
