# M0.2 Report

Partial. Added `QueryIndex` with outgoing/incoming adjacency, exact/qualified/file/partial lookup, and bounded BFS shortest path. CLI exposes `query` and `path`.

Implemented `explain` with location and incoming/outgoing relationships. Missing: richer ambiguity errors, dedicated query fixtures, and full traversal-limit diagnostics.